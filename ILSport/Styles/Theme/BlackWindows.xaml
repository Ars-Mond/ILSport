<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Colors.xaml"/>
        <ResourceDictionary Source="BaseStyle.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    
    <Style TargetType="Border" x:Key="BlackWindows.Window" BasedOn="{StaticResource BaseStyle.Border}">
        <Setter Property="Background" Value="{StaticResource ColorBrush.Gray6}"/>
    </Style>
    
    <Style TargetType="TextBlock" x:Key="BlackWindows.Title" BasedOn="{StaticResource BaseStyle.Headline1}">
        <Setter Property="Foreground" Value="{StaticResource ColorBrush.White}"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="40"/>
    </Style>
    
    <Style TargetType="TextBlock" x:Key="BlackWindows.Message.Error" BasedOn="{StaticResource BaseStyle.Footnote4}">
        <Setter Property="Foreground" Value="#F95050"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Margin" Value="40"/>
    </Style>
    
    <Style TargetType="Button" x:Key="BlackWindows.Button.Submit" BasedOn="{StaticResource BaseStyle.Button}">
    </Style>
    
    <Style TargetType="Button" x:Key="BlackWindows.Button.Back" BasedOn="{StaticResource BaseStyle.Button}">
        <Setter Property="Background" Value="#F95050"/>
    </Style>
    
    <Style TargetType="Button" x:Key="BlackWindows.Button.Link" BasedOn="{StaticResource BaseStyle.Button}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource ColorBrush.White}"/>
    </Style>
    
    <Style TargetType="TabControl" x:Key="BlackWindows.TabControl" BasedOn="{StaticResource BaseStyle.Footnote5}">
        <Setter Property="Background" Value="{StaticResource ColorBrush.Gray6}"/>
    </Style>
    
</ResourceDictionary>